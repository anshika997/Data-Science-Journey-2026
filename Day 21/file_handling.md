# File Handling in Python

## Introduction

File handling in Python allows us to create, read, write, and update files.
It is used to store data permanently on the system.

Python provides built-in functions to work with files easily.

---

## Opening a File

A file is opened using the `open()` function.

Syntax:
```python
file = open("filename", "mode")
